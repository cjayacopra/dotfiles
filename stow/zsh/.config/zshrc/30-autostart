# -----------------------------------------------------
# AUTOSTART
# -----------------------------------------------------

# The 'ff' command (Fuzzy Finder)
# finder() {
#     # Run the script and capture the result
#     local result=$($HOME/.config/ml4w/bin/ml4w-finder.sh)

#     # Check if the script told us to CD
#     if [[ "$result" == TYPE_DIR:* ]]; then
#         local target_dir="${result#TYPE_DIR:}"
#         cd "$target_dir" && ls
#     fi
# }

# # -----------------------------------------------------
# # Fastfetch (First-Prompt Hook)
# # -----------------------------------------------------
# if [[ $(tty) == *"pts"* ]]; then
#     # Load the hook utility
#     autoload -Uz add-zsh-hook

#     # Define a function to run fastfetch and then delete itself
#     _run_fastfetch_once() {
#         fastfetch --logo-type kitty
#         # Remove this hook immediately so it doesn't run after every command
#         add-zsh-hook -d precmd _run_fastfetch_once
#     }

#     # Attach the function to run right before the first prompt is drawn
#     add-zsh-hook precmd _run_fastfetch_once
# fi
