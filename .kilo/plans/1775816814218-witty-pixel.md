# Plan: Diagnose Noctalia Screenshot Plugin

## Problem
The screenshot plugin commands were not taking screenshots.

## Resolution

**RESOLVED**: The issue was that the required package `hyprshot` was not installed.

The screenshot plugin works correctly - it uses `hyprshot` for Hyprland screenshots. Once the user installed the missing package, the screenshot functionality works.